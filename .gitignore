#ignore all .a files
*.a

#but do track lib.a files even though you are ignoring .a files
!lib.a

#Only ignore the TODO file in the current directory, not in subdir/TODO
/TODO

#ignore all files in any files in a directory named build
build/

#ignore doc/notes.txt, but not doc/server/arch.txt
doc/*.txt

#ignore all pdf files in the doc/directory and many of its subdirectories
doc/**/*.pdf